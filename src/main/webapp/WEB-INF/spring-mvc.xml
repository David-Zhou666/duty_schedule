<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Enable annotation-driven MVC -->
    <mvc:annotation-driven/>

    <!-- Let the container's default servlet serve static resources (index.html, css, js) -->
    <mvc:default-servlet-handler/>
    <!-- Map static resources from webapp root so DispatcherServlet can serve them when matched -->
    <mvc:resources mapping="/**" location="/" />

    <!-- Component scanning -->
    <context:component-scan base-package="com.schedule"/>

    <!-- View resolver -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value=""/>
        <property name="suffix" value=".html"/>
    </bean>

    <!-- Multipart resolver: 使用 Commons FileUpload，避免依赖 Servlet multipart-config -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <!-- 限制上传大小为 20MB，可按需调整 -->
        <property name="maxUploadSize" value="20971520"/>
    </bean>

</beans>
